
var clPreviewInit = function() {

    /*
     * 1. iterate the list
     * 2. fetch the page for each link
     * 4. display thumbnails of pics
     * 5. some kind of lightbox
     */
    $('p.row').each(function() {
        // $(this).children
    })

}
